config {type: "view"}

-- select ${when(
--     dataform.projectConfig.warehouse === "bigquery",
--     "warehouse is set to bigquery!",
--     "warehouse is not set to bigquery!"
-- )}

SELECT "Testing dataform with Bigquery"